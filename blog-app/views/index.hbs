<h1>{{title}}</h1>
{{#each posts}}
    <div class="post">
        <h2>{{this.title}}</h2>
        <p>{{this.content}}</p>
<!--        <p>{{this.authorID}}</p>-->
        {{#each this.authors}}
            <p>By @{{this.username}}</p>
        {{/each}}
    </div>
{{/each}}
